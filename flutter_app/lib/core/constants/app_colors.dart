import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ===========================
  // Couleurs principales
  // ===========================

  /// Bleu principal iMotion
  static const Color primary = Color(0xFF3B82F6);

  /// Bleu clair (hover, accents)
  static const Color secondary = Color(0xFF60A5FA);

  // ===========================
  // Fond général
  // ===========================

  /// Fond principal de l'application
  static const Color background = Color(0xFF111827);

  /// Sidebar
  static const Color sidebar = Color(0xFF0F172A);

  /// Cartes
  static const Color surface = Color(0xFF1F2937);

  /// Variante légèrement plus claire
  static const Color surfaceLight = Color(0xFF263244);

  // ===========================
  // Bordures
  // ===========================

  static const Color border = Color(0xFF334155);

  // ===========================
  // Textes
  // ===========================

  static const Color textPrimary = Colors.white;

  static const Color textSecondary = Color(0xFF9CA3AF);

  static const Color textMuted = Color(0xFF6B7280);

  // ===========================
  // États
  // ===========================

  static const Color success = Color(0xFF4CAF50);

  static const Color warning = Color(0xFFFFA726);

  static const Color error = Color(0xFFEF5350);

  static const Color info = Color(0xFF29B6F6);

  // ===========================
  // Boutons
  // ===========================

  static const Color buttonPrimary = primary;

  static const Color buttonHover = Color(0xFF2563EB);

  // ===========================
  // Effets
  // ===========================

  static const Color shadow = Color(0x33000000);
}